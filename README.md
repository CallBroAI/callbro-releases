# callbro-releases

Signed macOS and Linux updater artifacts for [CallBro](https://github.com/CallBroAI/callbro-tauri-device),
published by that repo's release pipeline (`.github/workflows/release.yml`).

Windows ships through the Microsoft Store instead — see the source repo for details.
